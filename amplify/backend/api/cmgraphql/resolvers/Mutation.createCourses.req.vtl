#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCoursesInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCoursesInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO courses $colStr VALUES $valStr", "SELECT * FROM courses WHERE id=$ctx.args.createCoursesInput.id"]
}