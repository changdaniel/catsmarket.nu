#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCourseInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCourseInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Course $colStr VALUES $valStr", "SELECT * FROM Course WHERE id=$ctx.args.createCourseInput.id"]
}