#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCompletedInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCompletedInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Completed $colStr VALUES $valStr", "SELECT * FROM Completed WHERE id=$ctx.args.createCompletedInput.id"]
}