#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateConnectionInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateConnectionInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Connection SET $update WHERE id=$ctx.args.updateConnectionInput.id", "SELECT * FROM Connection WHERE id=$ctx.args.updateConnectionInput.id"]
}